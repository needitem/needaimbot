# simple_config.json 레퍼런스

`simple_config.json`은 섹션(`_section_*` 구분 키)으로 나뉜다. 구분 키는 순수 시각용이고 로더가 무시한다(모르는 키는 전부 무시). 아래는 섹션별 각 값의 의미 · 기본값 · 튜닝 노트.

> 값을 바꾸고 저장하면 다음 실행부터 적용된다. 파일이 없거나 엔진 경로가 정규화되면 프로그램이 이 포맷으로 다시 써준다.

---

> **2026-08-03 리그 실측 — sim 의 데드타임 모델이 2.6배 틀려 있었다.** 정지 표적 41초에
> 스텝 응답을 주입해 emit→visible 을 처음 직접 쟀다: **11.60ms**(143fps 수집).
> 부품 합산(USB 1.0 + 렌더대기 2.22 + E2E 3.20 = 6.42ms)이 놓친 ~5ms 는 **게임 자체의
> 렌더 파이프라인 지연**으로, 더해서는 알 수 없는 값이다. 두 추정기가 교차 확인했고
> (소수 적합 2.85 상관 0.69 / `calibrate.py` 정수 3프레임 상관 0.94), 7개 분할에서
> 표준편차 0.07 로 재현된다. 그때까지 sim 은 1.10 프레임을 가정했다 — **모든 튜닝이
> 실제보다 2.6배 빠른 플랜트를 상대로 이뤄졌다.**
> 실측 조건에서 다시 재보니 배포 중이던 설정은 오버슈트 **37.4**(sim 은 1.4 라고 믿었다),
> 획득 60.5ms 였다. 240Hz 모니터 교체로 프레임 주기까지 짧아진 게 겹친 결과다.
> 실측 조건 환산본으로 오버슈트 -94%, 획득 -64%, 총에러 -33%.
> 같이 드러난 것: 백색 노이즈는 sim 이 **5배 과대**(실측 X 1.69 / Y 1.13 vs 8.1/5.9),
> 드리프트는 **5배 과소**(6.99/5.91 vs 1.2), 결측률은 **7배 과소**(10.6% vs 1.5%).
> 다만 이 셋은 aim OFF·정지 표적·단일 거리의 값이라 aim ON 수집 전에는 sim 에 넣지 않는다.
> 재현: `tools/measure.sh deadtime` → `bench/deadtime_fit.py` + `bench/calibrate.py`.

> **2026-08-07 정정 — 그 환산이 246fps 를 가정했는데 실측은 206fps 였다.** 프레임간격의
> **중앙값**(4.38ms → 229fps)을 레이트로 읽은 게 원인이다. 중앙값은 긴 꼬리를 무시하므로
> 평균 px/초를 결정하지 못한다. 벽시계(201) · perf R 중앙(208) · **평균 간격(4.85ms →
> 206)** 셋이 일치하니 그게 맞는 값이다. 환산비가 0.581 이 아니라 **0.694** 였으므로
> 프레임 단위 값이 전부 19% 낮게 깔려 있었다 — 게인·`max_step`·`vgate` 는 19% 작고,
> `predict`·`ff_gain` 은 16% 크게. 실측 조건에서 다시 재면 **획득 40px 36.2 → 29.2ms
> (-19%), 오버슈트 0.70 → 0.66, 총에러 -2.1%** 로 트레이드 없이 개선된다(플랜트 데드타임
> 가정을 2.17/2.39 어느 쪽으로 잡아도 같은 방향).
> `thumb_*` 게인은 애초에 환산을 한 번도 안 받아 **1.44배 과게인**이었고 같이 정정했다.
> `inflight_deadtime_frames` 는 2.85 로 **보류**한다 — 11.60ms 는 143fps 에서 잰 값이고
> 그중 캡처 샘플링분(≈T/2)은 레이트를 따라 줄므로 206fps 환산치는 2.17(상한) 부근이지만,
> 낮출수록 빨라지고 오버슈트가 붙는 실제 트레이드라 추정치로 미끄러뜨릴 값이 아니다.
> **재측정한 뒤 정한다.** ms 는 옮길 수 있어도 프레임은 옮길 수 없다.

> **2026-08-07 재정정 — 그 206 도 대표성 없는 수집이었다. 실전은 231~237fps.**
> 206 은 정지 표적 테스트 값이고 간격 표준편차가 1.61 이었다. 클럭 고정 상태의 실전
> 플레이 401초는 **평균 4.22ms(237fps) · 표준편차 0.74**, 30초 구간별 226~246 으로
> 안정적이고 **교전이 레이트를 거의 안 떨어뜨린다(238→231)**. 컨트롤러는 조준 중에만
> 루프를 닫으므로 기준은 **231fps**, 환산비 **0.619** 다. 즉 정정 전 246 가정(0.581)이
> 오늘의 206 정정(0.694)보다 오히려 가까웠다 — 통계량은 고쳤는데 데이터가 실사용
> 조건이 아니었다.
> **다만 대가가 거의 없다.** 실제 플랜트(231fps, 데드타임 2.37프레임)에서 세 환산비의
> 총에러 폭이 2.1% 다. 게인 표면이 평평하다는 결과 그대로다.
> **움직이는 것은 `w` 뿐이다**: 2.85→2.37 이 획득 26.7→21.3ms(-20%), 오버슈트
> 0.69→1.97. 지금 배포본은 게인 과대(206 환산)와 `w` 과대(2.85)가 **서로 상쇄**돼
> 프론티어 중간에 앉아 있다(획득 24.2ms, 오버슈트 1.00) — 나쁘지 않지만 상쇄에 기댄
> 상태다. `w` 를 231fps 에서 실측한 뒤 환산비와 함께 정공법으로 맞춘다.

> **`config_version`** — 배포 기본값이 "기존 설정이 조용히 덮어쓸 수 있는" 방식으로
> 바뀔 때마다 올린다. 설정 파일의 값은 항상 코드 기본값을 이기므로, 튜닝을 코드에만
> 반영하면 이미 설치된 곳의 낡은 파일이 그 튜닝을 무효화한다(이 세션에서 우클릭 게인과
> thumb 게인 두 번 겪었다). 파일 버전이 낮으면 시작 시 크게 경고하고 갱신 방법을 알린다.
> **경고는 값을 바꾸지 않는다** — 파일이 여전히 이긴다. 조용한 실패를 보이게 할 뿐이다.
> `1` = 2026-08-01: 적응 데드타임 + 그에 맞춘 게인, thumb 최초 튜닝, `aim_h_ema`,
> `ego_frame_filter` 제거.
> `2` = 2026-08-06: 실측 데드타임(11.60ms)과 240Hz 환산. v1 파일이 남으면 옛 프레임
> 환산값이 새 플랜트에 그대로 쓰여 오버슈트 37 이 된다.
> `3` = 2026-08-07: 실측 레이트를 206fps 로 정정한 재환산 + thumb 최초 환산. v2 파일이
> 남으면 246 가정의 19% 낮은 게인이 그대로 쓰인다.
> `4` = 2026-08-07: `calibration_log_enabled` 분리. 값이 바뀌는 변경은 아니지만 키가
> 늘었으므로 올린다 — 없는 파일은 `perf_stats_enabled` 를 물려받아 옛 동작을 유지한다.
> `5` = 2026-08-07: **프레임레이트 런타임 정규화.** 설정값이 다시 143fps 기준 원본이
> 되고 런타임이 실측 주기로 환산한다. v4 파일이 남으면 이미 환산된 값을 또 환산해
> 게인이 0.6배가 된다 — 반드시 프리셋으로 갱신할 것.

## ENGINE / NETWORK
| 키 | 기본 | 의미 |
|---|---|---|
| `engine_path` | — | TensorRT 엔진(.engine) 경로. 파일명의 `320`/`fp16`이 입력 해상도·정밀도. |
| `udp_port` | 5007 | game_pc가 캡처 프레임을 보내는 UDP 포트. game_pc 송신 포트와 일치해야 함. |
| `makcu_port` | /dev/ttyACM0 | MAKCU 마우스 디바이스 시리얼 포트. |
| `makcu_baudrate` | 4000000 | MAKCU 보드레이트. 펌웨어와 일치해야 함. |

## DETECTION
| 키 | 기본 | 의미 |
|---|---|---|
| `conf_threshold` | 0.2 | 이 confidence 미만 탐지는 버림. 높이면 헛것↓·놓침↑. |
| `head_class_id` | 1 | head 클래스 ID(모델 기준). 머리 우선 선택·조준점 계산에 사용. |
| `max_detections` | 100 | 프레임당 디코드할 최대 박스 수(상한). |
| `allowed_classes` | [0,1] | 이 클래스만 타겟 후보로 사용(0=body, 1=head 등). |
| `pre_capture_shapes` | [[320,320]] | 시작 시 미리 CUDA 그래프를 캡처할 입력 해상도들. 들어오는 프레임 크기와 맞으면 콜드스타트 없음. |

## AIM POINT
조준할 "지점"을 박스 안 어디로 둘지. 0=박스 위(top), 1=아래(bottom) 방향 비율(모델 좌표 규약).
| 키 | 기본 | 의미 |
|---|---|---|
| `head_aim_point` | 1.0 | head 박스에서의 세로 조준 지점. |
| `body_aim_point` | 0.15 | body 박스에서의 세로 조준 지점(위쪽=가슴/목). |
| `shoot_offset_x` | 0.0 | **발사 중에만** 적용되는 조준점 가로 이동(setpoint shift). |
| `shoot_offset_y` | 0.0 | 발사 중 세로 조준점 이동(반동 상쇄용 소량). |
| `aim_h_ema` | 0.2 | body 조준점을 **같은 지점, 더 조용하게** 관측. `0`=끔(원래 식). 아래 참조. |
| `aim_y_scale` | 0.1 | **세로 보정 세기.** Y 출력 전체(P+D+리드)에 곱한다. `1.0`=튜닝된 그대로, `0`=가로 전용. 우클릭·thumb 두 프로필이 공유한다. 클램프 **뒤**, emit **앞**에서 곱한다. 순서가 직관과 반대다: `clampMaxStep`은 방향 보존을 위해 x·y를 같은 비율로 줄이므로 **Y를 먼저 누르면 벡터가 작아져 클램프가 덜 걸리고 X가 오히려 더 나간다** ((20,20)·max_step 19.56에서 x 13.8 → 19.5, 세로 노브가 가로를 +41%). 클램프를 먼저 하면 그 기계적 결합이 사라진다. emit 앞이어야 in-flight 링에 실제로 나간 Y가 기록돼 데드타임 보정이 어긋나지 않는다. 스케일 뒤에 **한 번 더 클램프**한다 — `1`을 넘는 값이면 상한을 넘어버리는데, 그 상한이 막으라고 있는 게 바로 프레임당 수직 튐이다. `1` 이하에서는 이 두 번째 클램프가 발동하지 않는다(랜덤 벡터 8만 개에서 0회; `1` 초과에서는 98% 발동)—즉 감쇠 구간은 추가 결합이 없다. 남는 결합은 'Y를 약하게 하면 Y 오차가 오래 남아 **첫** 클램프가 더 자주 걸린다'는 물리적인 것뿐이고, 폐루프 실측으로 가로 추적 오차 변화는 0.05%(잡음 수준)다. 게인은 건드리지 않으므로 튜닝 결과와 독립적인 취향 노브다. 패리티 게이트가 `0.1` 경로를 별도로 검증한다(1.0 만 보면 곱셈 위치 오류가 안 드러난다). |

**구조: body 조준점 관측식 (`aim_h_ema`)** — 조준점은 `y1 + k*h`인데 `h = y2-y1`이므로 사실 `(1-k)*y1 + k*y2`, 즉 **위·아래 엣지 노이즈를 둘 다** 물려받는다. 리그 400프레임 실측(크롭 160, 화면 px): 박스 지터는 **통째 이동(공통, σ≈2.5)** 과 **엣지별 독립(σ≈2.8)** 으로 갈리고, 독립 성분을 절반으로 평균하는 **중심이 박스에서 가장 조용한 점**(3.11)인 반면 현재 body 식(k=0.15)은 중심에서 멀어 3.49다. 같은 점을 `cy + (k-0.5)*h`로 다시 쓰면 대수적으로 동일하지만 빠른 성분은 조용한 중심이 담당하고 `h`는 따로 느리게 평활할 수 있다 — `h`는 사람 박스 높이라 거리에 따라 천천히 변하므로 무겁게 평활해도 지연 비용이 거의 없다.
실측: **σ_y 3.49 → 3.12 (-10.4%)** @ α=0.2, 지연 편향 0.92px(위상 지연 없는 기준 대비, σ의 1/4이라 묻힘). 폐루프 sim: 정지유지 -1.9%, 느린 스트레이프 -1.8%, 반전평균 -1.8%, 총에러 -1.3%, **오버슈트·획득 정확히 0.0% 변화**(속도를 전혀 팔지 않는다).
head는 이득이 **없다**(k=0.601이 이미 중심 0.5 근처라 실측 +1%) — 그래서 body 앵커에만 적용된다. 트랙이 새로 잡히거나 head↔body 플립이 나면 `h_ema`를 다시 씨앗 심는다(다른 박스의 높이를 이어받으면 조준점이 그 차이만큼 끌려간다).

## AIM CONTROLLER + CENTER FILTER

> **프레임 단위 값은 이제 런타임이 환산한다 (`aim_rate_adaptive`, v5).** 아래 표의 숫자는
> **`aim_tuned_fps`(143) 에서의 값**이고, 실제로 커널에 올라가는 값은 매 프레임 실측
> 프레임주기로 환산된 것이다. 231fps 에서는 `kp` 0.765→0.474, `max_step` 19.56→12.11,
> `predict` 3.18→5.14, 데드타임 1.66→2.37 프레임이 된다. 시작 시와 레이트가 5% 넘게
> 움직일 때 `[Rate]` 로 실제 값을 찍는다.
> **왜:** 캡처 레이트가 고정이 아니다 — 한 세션 안에서 226~246 으로 움직였고, 하루에
> 잰 세 수집이 199·206·237 이었다. 손으로 환산하다 하루에 세 번 틀렸고, 그 중 한 번은
> 이미 맞던 값을 12% 어긋나게 만들었다. 사람이 기억해야 하는 방식은 이미 실패했다.
> 끄려면 `aim_rate_adaptive: false` — 그러면 표의 숫자가 그대로 쓰인다(143fps 전용).
크로스헤어를 타겟으로 밀어붙이는 제어기. 우클릭용(`aim_*`)과 사이드버튼(thumb)용 게인이 분리돼 있다.

**PD 게인** — `kp`=비례(당기는 세기), `softness`=비선형 P의 부드러움(클수록 원거리에서 순함), `kd`=미분(오버슛/떨림 억제).
| 키 | 기본 | 의미 |
|---|---|---|
| `aim_kp_x` / `aim_kp_y` | 0.765 / 0.698 | 우클릭 조준 비례 게인(가로/세로). **`inflight_comp`가 켜져 있어야 이 값이 안전하다** — 보정 없이 이 게인이면 데드타임 링잉이 난다(보정 OFF의 안전값은 ~0.55). |
| `aim_softness_x` / `_y` | 8.6 / 6.57 | 비선형 P 소프트닝. 클수록 먼 거리에서 완만. |
| `aim_kd_x` / `_y` | 0.052 / 0.037 | 미분 게인. **주의: D항은 낡은 오차에 반응하므로 데드타임 하에선 감쇠가 아니라 링잉을 키운다**(실측 kd 0.18→0.10에서 오버슈트 0.5→0.3회). `inflight_comp`가 데드타임을 처리하므로 D는 거의 불필요 — 낮게 유지할 것. |
| `thumb_aim_kp_x`/`_y` | 0.709 / 0.408 | thumb(사이드버튼) 조준 비례 게인. |
| `thumb_aim_softness_x`/`_y` | 4.66 / 9.77 | thumb 소프트닝. |
| `thumb_aim_kd_x`/`_y` | 0.02 / 0.082 | thumb 미분 게인. **2026-08-01 최초 튜닝** — 그전까지 0.18/0.22 로 우클릭 프로필의 4~5배였다. 프로필별로 갈리는 값은 이 6개뿐인데 우클릭만 반복 재최적화되고 thumb 은 초기값에 남아 있었다. 데드타임 하에서 큰 D는 감쇠가 아니라 과감쇠다: 오버슈트는 0.74로 낮았지만 그 대가로 t4 11.00(우클릭은 9.85), 획득 5.49. 튜닝 후 **총에러 -7.5%, t4 -10.4%, 반전피크 -3.0%, 획득 -2.6%**, 오버슈트는 1.22로 우클릭 프로필(1.21)과 같은 수준. |

**Target tracking / stickiness**
| 키 | 기본 | 의미 |
|---|---|---|
| `iou_stickiness_threshold` | 0.3 | 이전 프레임 타겟과 IoU가 이 이상이면 같은 타겟으로 유지(타겟 스위칭 억제). |
| `distance_stickiness_factor` | 0.4 | 거리 기반 스티키니스 가중(0=끔). 켜면 가까운 이전 타겟을 더 붙잡음. |
| `track_persistence_frames` | 3 | 탐지 끊겨도 이 프레임 수만큼 트랙 유지(공백 동안 크로스헤어는 정지, 재획득 대비). |
| `inflight_comp` | 1.0 | **데드타임 보정(Smith predictor).** 검출은 `inflight_deadtime_frames`만큼 낡았으므로, 그 사이 내보낸 이동은 아직 측정에 안 나타난다. 그걸 빼지 않으면 **같은 오차에 두 번 반응**해 오버슈트·링잉이 난다. 0=끔(레거시), 1=완전 보정. 실측: 스텝 오버슈트 12.7px→0.4px, 정착 7→4프레임. 이상하면 0으로 즉시 되돌릴 수 있다. |
| `inflight_deadtime_frames` | 2.85 | 보정에 쓸 emit→visible 지연(프레임). **정수가 아니어도 된다(float)** — 실제 지연이 정수 프레임일 이유가 없어서 반올림하면 과소/과대보정이 된다. 소수부는 그 이전 프레임 emit에 가중된다. `1.0`은 옛 정수 동작과 완전히 동일. **리그 실측값을 넣을 것** — `bench/deadtime_fit.py`(`calibration_step_px>0`; 소수 프레임까지). 과대 추정하면 과보정되어 진동한다. |
| `aim_max_step` | 19.56 | 프레임당 최대 이동(px). 급격한 튐 방지 상한. 0=무제한. 저신뢰 검출(conf~0.2)이 만드는 간헐적 수직 튐의 **크기 상한** 역할 — 낮출수록 튐이 잘리지만(30→23px, 15→15px) 큰 보정이 굼떠진다. 정상 추적 이동량은 수 px라 25에서도 클리핑되지 않는다. |

## LEAD (ego-corrected velocity feedforward)
비례(P) 제어는 이동 표적에 대해 **구조적 정상상태 램프 랙**을 남긴다(게인으로 못 없앰). 이 항이 그걸 상쇄한다. 과거 ff는 화면좌표 속도가 에고모션에 오염돼 실패했지만, in-flight 링이 우리가 내보낸 이동을 정확히 알고 있으므로 `dT = d_raw + ring[ff_ego_lag]`로 **표적의 진짜 화면 속도**를 복원할 수 있다(그때는 링이 없었다).

| 키 | 기본 | 의미 |
|---|---|---|
| `ff_gain` | 1.7 | 리드 세기. `0`=끔(순수 P+D, 이전 동작). sim(실측 노이즈): 빠른표적 랙 -12%(4px/f)·-15%(8px/f), 스텝 오버슈트·진동 불변. |
| `ff_ego_lag` | 2.85 | 직전 두 검출 사이 우리 이동이 들어있는 링 지연(프레임). `inflight_deadtime_frames`와 함께 움직이게 유지한다. **다만 레버는 아니다** — 실측 조건에서 2.25~4.0(링 깊이 한계)을 훑어도 총에러가 10.800~10.849로 0.5% 안에 들어온다. 그 구간에서 오버슈트는 2.23→1.73으로 줄고 t8은 16.87→17.87로 늘어 서로 상쇄된다. 값이 크게 어긋나면 과거 에고 시도처럼 실패하지만, 데드타임 근처라면 정확한 값은 중요하지 않다. |
| `ff_v_ema` | 0.235 | 속도 추정 EMA 가중(0~1). 높이면 반응↑·노이즈↑. |
| `lead_vgate` | 14.79 | **속도 게이트**(두 리드 항 `ff_gain`·`predict_frames`가 **공유**): `g *= |v|/(|v|+vgate)`. 정지 시 ~0이라 노이즈 섞인 속도 추정이 홀드를 흔들지 못한다. 낮추면 이득↑·정지지터↑. |
| `lead_err_gate` | 22.25 | **오차 게이트**(두 리드 항이 **공유**): `g *= e0²/(e0²+|err|²)`. 획득 중(오차 큼)엔 리드를 끈다 — 위치 점프(신규 락온/타겟 스위치)의 1프레임 거대 속도를 피드포워드하면 오버슈트가 난다. 이동 표적은 오차가 작고 속도가 지속되므로 통과. `0`=오차 게이팅 없음. |

> **2026-08-01: 검출 공백 프레임에서 in-flight 링을 전진시킨다.** 공백에는 아무것도
> 내보내지 않는데 링이 멈춰 있어서, 공백 전 이동이 계속 "지연 1프레임" 자리에 남아
> 재획득 프레임에 이미 보이게 된 이동을 다시 뺐다(과보정). sim: 실측 결측률 1.5%에서
> 에러 -0.1%, 10%에서 -1.0%(t8 -3.5%) — **검출이 나쁠수록 더 이득**. 같이 검토한
> 두 가지는 기각: 공백 길이로 속도를 나누는 정규화는 오히려 +0.2~0.8%(과대추정된
> 속도가 공백 동안 벌어진 거리를 메우고 있었다), 필터 alpha·표적전진 보정은 ±0.1%.

**구조: 프레임별 적응 데드타임 (`deadtime_adaptive`)** — `inflight_comp`는 아직 보이지 않은(=비행 중인) 우리 이동을 빼서 이중보정을 막는데, **몇 프레임치를 뺄지가 고정 상수 `inflight_deadtime_frames`**였다. 실제 전송 지연은 프레임마다 흔들리므로(실측 E2E p50 2.7ms / p95 4.8ms) 대부분의 프레임에서 틀린 양을 빼고 있었고, **그 오차는 게인으로 없앨 수 없는 오버슈트**로 나온다. 그런데 그 프레임이 얼마나 낡았는지는 제출 시점에 이미 안다(캡처 타임스탬프 vs 현재). 이걸 먹이면 GPU 비용이 0이다 — `deadtime_frames`는 이미 소수이고 프레임마다 업로드된다.
`true`(기본)면 **자기 세션의 평균 대비 편차만** ±0.75프레임 한도로 적용한다(고정 클럭오프셋 편향은 상쇄되고, 나쁜 샘플 하나가 루프를 흔들지 못한다).
sim(4블록×60): 같은 게인에서 **스텝 오버슈트 -34%**, 획득40 -1.7%, 에러 -0.4% — **나빠지는 지표 0**. 이 세션에서 처음으로 속도↔링잉 곡선 '위를 미끄러진' 게 아니라 곡선 자체가 옮겨간 변경이다.
⚠️ **현재 게인은 이 기능을 켠 상태로 최적화됐다.** 끄면 오버슈트가 1.23→1.99로 튄다(측정 잡음이 신호와 같은 크기여도 1.42로 버티지만, 완전 실패는 다르다). 끌 거면 게인도 함께 되돌릴 것.

| 키 | 기본 | 의미 |
|---|---|---|
| `deadtime_adaptive` | true | 프레임별 실측 낡음으로 `inflight_deadtime_frames`를 보정. `false`=고정 상수. **game_pc 와의 시계 오프셋이 있어야 동작한다** — 프레임의 낡음을 우리 시계로 재려면 필요하다. 오프셋은 시계 동기 핑으로 얻는데, 그 핑이 원래 E2E 지연 통계용이라 `perf_stats_enabled` 뒤에 갇혀 있었다(2026-08-07 수정). 그래서 **측정 중이 아닌 모든 실행에서 이 기능이 조용히 꺼져 있었다** — 정작 필요 없는 때만 켜져 있었던 셈이고, 게인은 켜진 걸 전제로 튜닝돼 있었다. 이제 오프셋을 2000프레임 동안 못 받으면 경고를 찍는다(조용한 실패가 이걸 숨긴 원인이라서). |

**구조: 대칭 데드타임 보정 (`predict_frames`)** — `inflight_comp`는 **우리** 이동만 뺐고, 같은 데드타임 동안 **표적**이 움직인 건 아무도 보정하지 않았다(반쪽 보정). 표적을 `predict_frames * v`만큼 전진시켜 대칭을 완성한다. `ff_gain`은 출력에 더하지만 이건 **설정점(setpoint)** 을 옮기므로 비선형 P·max_step을 그대로 통과한다. 둘은 상보적이라 함께 쓰면 각각보다 낫다.

| 키 | 기본 | 의미 |
|---|---|---|
| `predict_frames` | 3.18 | 표적 전진 프레임. `0`=끔. 물리 데드타임보다 크면 순수 예측기가 아니라 **튜닝된 리드**로 동작 — 의도된 것이고 게이트가 안전을 담보한다. |

**앵커 플립 처리 (`class_switch_reject`)**
| 키 | 기본 | 의미 |
|---|---|---|
| `class_switch_reject` | true | head↔body 플립 프레임을 **`classId`로 정확히 식별**해(임계값 추측 불필요) **속도 추정에서만** 제외한다. 두 앵커는 같은 적의 다른 지점이라 플립 시 중심이 11~21px 튀는데, 속도 추정에는 그게 순수 아티팩트다(리드가 튄다). **D항은 일부러 그대로 본다** — 플립 후 조준점은 실제로 옮겨간 것이라 그 오차 계단은 진짜 설정점 변경이고 D가 한 번 반응하는 게 맞다(억제하면 두 게인 프로필 모두에서 0.03~0.22% 나쁨). 80 held-out seed: 에러 -0.9%, 반전피크 -1.5%, hold -1.2%, 나빠지는 지표 0. |

**One Euro는 가볍게(`oneeuro_min_cutoff` 0.1→0.3)** — 리드 항이 반응성을 공급하므로 저역통과의 위상 지연이 더는 필요 없다. 이것만으로 **스텝 오버슈트 3.7→2.5px(-33%)**. 반대로 완전히 끄면(OFF) 오버슈트는 0.9까지 가지만 hold·reach 비용이 커서 0.3이 무릎점.

> 튜닝 근거·재현: `bench/aim_opt.py`(실측 노이즈 σ 8.1/5.9 + 프랙셔널 지터 데드타임 1.13±0.5 주입), `bench/parity/`(커널↔모델 일치 게이트). 60개 held-out seed(700-759) 최종 검증 — **P+D 대비 총에러 -12.4%, 스텝 오버슈트 3.74→2.49px(-33%), osc 0.00 유지**, 빠른표적 t4 -17%·t8 -24%, reach +2%. 대가: 정지 hold +10%, 반전 피크 +4.8%. head↔body 클래스 전환(지속 11~21px 점프, 2% 프레임) 주입 리스크 테스트에서도 max 에러 +6%에 그치고 빠른추적 -14% 유지 — 이중 게이팅이 스파이크를 억제함.

> 옛 `feedforward_gain` / `feedforward_vgate` / `predict_horizon`은 제거됐다. 그건
> **raw 화면좌표 속도**에 얹혀 있었고 그 신호는 에고모션(조준 자체의 카메라 회전)에
> 오염돼 정상상태 추적에서 0이 되므로 램프 지연을 못 지우고 노이즈만 증폭했다.
> 위 LEAD 섹션(`ff_gain`/`predict_frames`)은 **다른 신호**를 쓴다 — in-flight 링으로
> 에고 성분을 되돌린 속도. 당시 없던 그 인프라가 재도입의 선행조건이었다.

**One Euro center filter** — 탐지 중심의 고주파 지터 억제. 속도 적응형 저역통과.
| 키 | 기본 | 의미 |
|---|---|---|
| `oneeuro_enabled` | true | 필터 on/off. 끄면 raw 중심 사용(더 즉각적이나 떨림↑). |
| `oneeuro_min_cutoff` | 0.084 | 최소 컷오프. 낮을수록 정지 시 부드럽지만 **위상 지연→오버슈트↑**. 리드 항 도입 후 0.1→0.3으로 **가볍게** 한 것이 스텝 오버슈트 -33%의 주 요인. 완전 OFF는 오버슈트 최저지만 hold·reach 비용이 커서 0.3이 무릎점. |
| `oneeuro_beta` | 0.02 | 속도 적응 강도. 높이면 빠른 움직임에서 지연↓(덜 부드러움). |

> **제거됨 (2026-08-01): `ego_frame_filter`** — 측정을 에고 없는 좌표계로 올려 필터링하면
> 스텝 오버슈트 -68% 였지만 **획득이 40~80% 느려졌다**(빠른 획득과 오버슈트는 같은
> 메커니즘이라 트레이드였다). 이 리그의 구속 조건은 속도라 켤 일이 없었고, 링잉 감소라는
> 목적은 `deadtime_adaptive`(오버슈트 -34%, 획득 -1.7%)가 획득 비용 없이 대체했다.
> 커널 분기·설정 키·패리티 인자까지 전부 걷어냈다.

## RECOIL COMPENSATION
`no_recoil_enabled=false`면 미사용.
| 키 | 기본 | 의미 |
|---|---|---|
| `no_recoil_enabled` | false | 발사 중 반동 상쇄 tick 활성. |
| `recoil_comp_x` / `recoil_comp_y` | 0.0 / 3.0 | tick당 보정 이동(가로/세로 counts). |
| `recoil_tick_ms` | 8 | 보정 tick 주기(ms). |

## PIPELINE / LATENCY
| 키 | 기본 | 의미 |
|---|---|---|
| `max_inflight_frames` | 1 | GPU에 동시에 올릴 프레임 수. 1=최저 지연(오버슛 방지), ↑=처리량↑·지연↑. |
| `frame_credit_depth` | 2 | 프레임 크레딧 깊이(수신-제출 흐름 제어). |
| `mouse_min_interval_ms` | 0 | 마우스 이동 최소 간격(레이트 리밋). 0=무제한. |
| `direct_aim_move_in_callback` | true | GPU 콜백 스레드에서 바로 마우스 전송(지연↓). |
| `inference_keepwarm_ms` | 0 | **조준 안 할 땐 추론을 끄는데(전력 절약), 조준 뗀 뒤 이 ms만큼 더 돌려 warm 유지.** `0`=엄격(재조준마다 콜드 1사이클 지연), `>0`(예 300)=교전 중 재조준 즉시, `<0`=항상 켜짐(콜드 지연 0, 유휴 GPU 부하). |
| `udp_busy_spin` | false | 수신 소켓을 커널 대기 대신 **busy-poll**(MSG_DONTWAIT 스핀). 프레임 첫 패킷의 IRQ→웨이크업 지연(~5-15µs) 제거. 대가: 수신 코어(affinity_core_receive) 100% 점유. Linux 전용. |
| `idle_graph_precapture_enabled` | true | 유휴 시 미리 CUDA 그래프 캡처(첫 조준 콜드스타트 제거). |
| `idle_graph_precapture_interval_ms` | 100 | 유휴 프리캡처 시도 주기(ms). |

## SYSTEM (threads / CPU affinity)
| 키 | 기본 | 의미 |
|---|---|---|
| `realtime_threads_enabled` | true | 실시간 스케줄링 우선순위 사용. |
| `cpu_affinity_enabled` | true | 스레드를 특정 코어에 고정. |
| `affinity_core_main` | 5 | 메인(프레임 취득+제출) 루프 코어. |
| `affinity_core_receive` | 7 | UDP 수신 스레드 코어. |
| `affinity_core_callback` | 6 | GPU 완료 콜백(마우스 전송) 코어. |
| `affinity_core_sender` | 4 | 송신 스레드 코어. |

## DIAGNOSTICS (perf log / calibration / bench)
평상시엔 대부분 off. **로그 스위치는 둘로 갈라져 있다** — 서로 다른 질문에 답하기 때문이다.
`perf_stats_enabled`는 *우리 파이프라인 어디서 시간을 쓰나*, `calibration_log_enabled`는
*검출기가 뭘 봤고 우리가 뭘 내보냈나*. 프레임 타임스탬프는 calib CSV 에 있으므로 **실측
fps 는 calib 만 켜도 나온다** — 플레이 검증이 perf 를 안 켜는 이유다(`tools/measure.sh play`).
v3 까지는 `perf_stats_enabled` 하나가 둘 다 켰다. 측정할 땐 늘 둘 다 필요해서 구분할
이유가 없었을 뿐이다.
| 키 | 기본 | 의미 |
|---|---|---|
| `perf_stats_enabled` | false | 파이프라인 단계 타이밍·지연 퍼센타일 → `perf_log_path`. **v4부터 calibration 로그와 무관.** |
| `calibration_log_enabled` | false | 검출/출력 추적 CSV → `calibration_log_path`. **키가 없는 v3 이하 파일은 `perf_stats_enabled` 값을 물려받는다** — 옛 파일의 뜻이 조용히 바뀌어 원하던 수집이 안 되는 게 로그 하나 더 쓰는 것보다 나쁘다. 비용은 사실상 0(미리 예약된 버퍼에 push_back, 종료 시 일괄 덤프)이지만 **버퍼가 20만 행**이라 206fps 에서 16분이면 차고, 차면 경고를 찍고 그 뒤로는 기록하지 않는다. |
| `perf_stats_interval_ms` | 1000 | 통계 출력 주기. |
| `perf_log_path` | perf_stats.log | 통계 로그 파일. |
| `perf_log_max_bytes` | 33554432 | 로그 회전 최대 크기(32MB). |
| `perf_log_truncate_on_start` | false | 시작 시 로그 비우기. |
| `stage_timing_enabled` | false | 파이프라인 단계별 세부 타이밍. |
| `force_aim_on` | false | **벤치마크 전용.** 조준키 없이 항상 조준 활성(측정용). 실사용 금지. |
| `calibration_log_path` | calib.csv | 캘리브레이션 CSV 경로(**어디에 쓸지**만 결정, 켜는 건 `calibration_log_enabled`). **상대 경로는 앱의 작업 디렉터리 기준**이고 `needaimbot.sh`가 거기를 `inference_pc/`로 옮긴다 — 바이너리 옆이 아니다. `tools/measure.sh`는 절대 경로를 넣는다. `bench/calibrate.py`로 분석. |
| `calibration_step_px` | 0 | >0이면 스텝응답 dead-time 측정용 마우스 펄스 주입(±px). 조준 OFF·정지 타겟에서만. 0=끔. |
| `calibration_step_period_ms` | 250 | 스텝 주입 주기(ms). |
